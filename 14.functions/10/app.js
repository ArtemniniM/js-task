// Даны две строковые переменные. Необходимо:

// В первом console.log объединить их через пробел с помощью конкатенации
// Во втором console.log объединить через пробел с помощью интерполяции строк
// Редактор форматированного текста

const a = "kak";
const b = "dela";

console.log(a + " " + b);
console.log(`${a} ${b}`);
