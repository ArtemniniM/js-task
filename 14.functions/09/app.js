// На вход подаётся числовая переменная. Необходимо вывести четыре разных результата с помощью console.log:
// Квадратный корень числа (используя Math.sqrt)
// Число, округлённое в меньшую сторону (используя Math.floor)
// Число, округлённое в большую сторону (используя Math.ceil)
// Число, округлённое по правилам математики (используя Math.round)

let a = 9.6;

console.log(Math.sqrt(a));
console.log(Math.floor(a));
console.log(Math.ceil(a));
console.log(Math.round(a));
