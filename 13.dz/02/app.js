// Создать новый массив из квадратов чисел, используя map.
// Тест-кейсы:
// [1, 2, 3] → [1, 4, 9]
// [0, -2, 5] → [0, 4, 25]
// [] → []

const arr = [1, 2, 3];

const res = arr.map((el) => el ** 2);

console.log(res);
