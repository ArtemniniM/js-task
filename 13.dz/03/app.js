// Отфильтровать только положительные числа, используя filter.
// Тест-кейсы:
// [1, -2, 3, 0] → [1, 3]
// [-5, -3, -1] → []
// [10, 20, 30] → [10, 20, 30]

const arr = [-5, -3, -1];

const res = arr.filter((el) => el > 0);
console.log(res);
