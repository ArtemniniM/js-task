// .push() и .pop() Задача:

// Добавьте число 4 в конец массива [1, 2, 3] с помощью push().
// Удалите последний элемент из получившегося массива с помощью pop().

const arr = [1, 2, 3];
arr.push(4);
arr.pop();
console.log(arr);
