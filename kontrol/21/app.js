// Проверьте, есть ли число 5 в массиве [1, 3, 5, 7], используя метод includes().

// Редактор форматированного текста

const arr = [1, 3, 5, 7];

arr.includes(5) ? console.log(true) : console.log(false);


