// Переберите массив [10, 20, 30] с помощью цикла for...of и выведите каждый элемент в консоль.

// Редактор форматированного текста

const arr = [10, 20, 30];

for (let el of arr) {
  console.log(el);
}
