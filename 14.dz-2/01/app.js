// Напиши функцию, в которую при вызове передаётся аргумент name, принимается
//  один параметр name и возвращает строку: Привет, <name>!

function DOname(name) {
  return `Привет, ${name}!`;
}
console.log(DOname('artem'));

