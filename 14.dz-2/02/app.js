// Напиши функцию, которая принимает два параметра: name и age, и возвращает строку вида: <name>, вам <age> лет.

const doInc = (name, age) => {
  return `${name},вам ${age} лет`;
};
console.log(doInc("Artem", 22));
